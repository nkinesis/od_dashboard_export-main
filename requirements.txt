# OD dashboard — Python dependencies
# Install (from project root):
#   python -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# Refresh this file after installing new packages:
#   pip freeze > requirements-lock.txt
#   (then copy only what this project needs into requirements.txt)

# --- Run the dashboard (required) ---
#   python scripts/run_dashboard.py --bundle-root .
Flask==3.1.3
flask-cors==6.0.5
psycopg2-binary==2.9.12

# --- Bundle pack/unpack (optional; source machine only) ---
#   python scripts/bundle_od_dashboard.py unpack --bundle-dir .
# Needs scripts/db_utils.py from the full PopGen repo for "pack".
SQLAlchemy==2.0.51
greenlet==3.5.1
